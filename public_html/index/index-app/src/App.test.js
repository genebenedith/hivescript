import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  render(<App />);
  // {this.state.shouldRedirect ? (
  //   <Navigate replace to="../../account/homepage/home.html" />
  // ) : null}
});
